#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const course_1 = require("@tutors-sdk/tutors-lib/src/models/course");
const json_emitter_1 = require("./src/controllers/json-emitter");
const netlify_1 = require("./src/controllers/netlify");
const tutors = require('./package.json').version;
const tutors_lib = require('./package.json').dependencies['@tutors-sdk/tutors-lib'];
const version = `tutors-json ${tutors} (tutors-lib: ${tutors_lib})`;
if (fs.existsSync('course.md')) {
    const course = new course_1.Course();
    let folder = process.cwd() + '/json';
    console.log(`Static course generator ${version}`);
    course.publish(folder);
    const emitter = new json_emitter_1.JsonEmitter();
    emitter.generateCourse(version, folder, course);
    (0, netlify_1.generateNetlifyToml)(folder);
    console.log(` ${version}`);
}
else {
    console.log('Cannot locate course.md. Please Change to course folder and try again. ');
}
//# sourceMappingURL=tutors-json.js.map